@page
@model SpotifyPlaylisterApp.Pages.SpotifyUsers.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>SpotifyUser</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SpotifyUser.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SpotifyUser.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SpotifyUser.Playlists)
        </dt>
        <dd class="col-sm-10">
            @foreach (var p in Model.SpotifyUser.Playlists){
                <div><a asp-page="/Playlists/Details" asp-route-id=@p.Id>@p.Name</a></div>
                //@Html.DisplayFor(model => model.SpotifyUser.Playlists)
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.SpotifyUser.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
